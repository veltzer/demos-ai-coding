The prompts given to Claude Code to create the examples in this folder, in
order, quoted verbatim. Each entry states which file the prompt created (or
modified).

-----------------------------------------------------------------------------
created: simple_1.js (plus package.json with @anthropic-ai/sdk)

    look at ../airline-ticket-agent-py/simple_1.py and write the equivalent
    on nodejs in this folder as simple_1.js as a node application. We will
    need some library or use pass(1) directly to get a hold of my api key.

-----------------------------------------------------------------------------
created: simple_2.js

    good. now write simple_2.js and do not hardcode the question to ask
    claude. Instead, in a loop, ask the user to input a question, send it to
    claude, get the response, print the response and look back. program
    terminates when the user pressed CTRL+D.

-----------------------------------------------------------------------------
created: simple_3.js -- later renamed by hand to simple_4.js

    now do simple_3.js. In this example the loop stays but I want it to only
    answer quetions which are somewhat related to airline ticket sales. If
    the user asks for other stuff answer that you can only answer quetsions
    related to airline ticket sales.

-----------------------------------------------------------------------------
created: simple_3_b.js -- later renamed by hand to simple_3.js

    now do simple_3_b.js which is like simple_3.js but you ask the LLM
    twice: once you ask the LLM is the quetsion is related to flights and
    only if the LLM says "yes" you ask it to really answer the question and
    print the answer. If the LLM says "no" you, and not the LLM, print that
    you cannot answer questions not related to flight

-----------------------------------------------------------------------------
created: simple_5.js (first written as simple_4.js, then moved after the
prompt below it)

    now I want to actually enable the user to buy tickets. In order to buy a
    ticket the user needs to specific 3 things: departure_city, arrival_city
    and date. Once the converstaion has these three pieces of informatoin,
    you can call a method to "buy_the_ticket" which will only print "buyiung
    the ticket with data: ...." on the screen.

    return simple_4.js to its previous state and write the new example in
    simple_5.js

-----------------------------------------------------------------------------
created: simple_6.js

    now simple_6.js. Add a /clear command that will clear the conversation
    history anbd allow the user to start from scratch

-----------------------------------------------------------------------------
created: simple_7.js (the second prompt arrived while the first was being
implemented and added the [agent]/[llm] debug prints)

    Stage 7: let the model read a preferences file the user names.

    If the user's message contains a filename, the model is offered a
    `read_preferences` tool for that turn. The client -- not the model --
    does the reading, and only for a filename the user actually typed, so
    the model cannot reach a file the user did not ask for.

    add debuggin prints so that I could see when the agent does something
    and when the llm does something

-----------------------------------------------------------------------------
created: index_docs.js (LanceDB was chosen in a preceding discussion:
"What small vector db would you recommend to be used by our agent in the
next example?")

    first write a script that indexes all the documents in ./docs into the
    LanceDB.

-----------------------------------------------------------------------------
created: search_docs.js

    now write a small script that allows me to search the vector db

-----------------------------------------------------------------------------
created: simple_8.js

    Now I want you to add RAG capabilities in the 8'th stage. Whenever you
    ask the LLM a question attach the vector db findings about that
    question.

-----------------------------------------------------------------------------
modified: simple_7.js and simple_8.js (broadened the system prompt beyond
pure ticket sales)

    modify the prompt of simple_7 so as to allow answering about issues
    around flights: bagagge, things to bring on-board etc. Same for
    simple_8

-----------------------------------------------------------------------------
created: prompts.txt

    write all the prompts for the various parts in a file called
    prompts.txt and state for each primpt which example it created
